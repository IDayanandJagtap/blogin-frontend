import { Box, HStack } from "@chakra-ui/react";
import React from "react";

const Footer = () => {
    return (
        <Box maxW={"full"} bgColor={"gray.100"} h={64} mt={20}>
            <HStack></HStack>
        </Box>
    );
};

export default Footer;
